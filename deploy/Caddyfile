# Reverse proxy for the API host (e.g. api.andreisili.com).
# Caddy obtains and renews a Let's Encrypt certificate automatically and
# transparently proxies both HTTP and WebSocket (/ws/) traffic to Daphne.
{$API_DOMAIN} {
	encode zstd gzip
	reverse_proxy backend:8000
}
